<?xml version="1.0" encoding="utf-8"?>

<!--
SPDX-FileCopyrightText: © 2026 Logan Magee

SPDX-License-Identifier: AGPL-3.0-only
-->

<manifest
        android:versionCode="2"
        android:versionName="2.0"
        package="com.example.valid"
        xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
    The uses-permission declarations here are intentionally placed in noncontiguous order since 1)
    compiled Android manifests sometimes have this structure, 2) Android properly recognizes this
    structure, and 3) we must properly parse this structure (i.e., collect _all_ permission
    declarations) to prevent permission review bypasses.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-sdk android:minSdkVersion="33" android:targetSdkVersion="36"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/Theme.App">
        <activity
                android:name=".MainActivity"
                android:exported="true"
                android:theme="@style/Theme.App">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
